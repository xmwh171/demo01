version: '3.8'
services:
  # 项目服务
  app:
    build: .
    container_name: springboot-demo01
    ports:
      - "8080:8080"
    depends_on:
      - mysql
      - redis
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/demo?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=123456
      - SPRING_REDIS_HOST=redis
      - SPRING_REDIS_PORT=6379
    volumes:
      - ./logs:/app/logs  # 日志挂载到本地
    restart: always

  # MySQL服务
  mysql:
    image: mysql:8.0
    container_name: demo-mysql
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=123456
      - MYSQL_DATABASE=demo  # 自动创建数据库
      - MYSQL_CHARACTER_SET_SERVER=utf8mb4
    volumes:
      - ./mysql-data:/var/lib/mysql  # 数据持久化
    restart: always

  # Redis服务
  redis:
    image: redis:7.0
    container_name: demo-redis
    ports:
      - "6379:6379"
    volumes:
      - ./redis-data:/data  # 数据持久化
    command: redis-server --appendonly yes  # 开启AOF持久化
    restart: always